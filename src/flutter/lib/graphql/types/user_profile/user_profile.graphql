fragment UserProfileFields on UserProfile {
  fields {
    name
    isRequired
    title
    options {
      name
      value
    }
    selectedOptions {
      name
      value
    }
    isMultiple
  }
}
